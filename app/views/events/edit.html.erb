<h1>Editing event</h1>

<%= form_for(@event) do |f| %>
  <%= render :partial => 'event_form', :locals => {:f => f} %>
  <p>
    <%= f.submit "Update" %>
  </p>
<% end %>

<%= link_to 'Show', @event %> |
<%= link_to 'Back', events_path %>
